#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../override/bin/node-override" "$@"
  ret=$?
else 
  node  "$basedir/../override/bin/node-override" "$@"
  ret=$?
fi
exit $ret
